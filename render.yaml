services:
  - type: web
    name: riskguard-api
    runtime: python
    plan: free
    autoDeploy: true
    buildCommand: pip install -r requirements.api.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: USE_ML_ANOMALY
        value: "false"
